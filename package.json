{
  "dependencies": {
    "firebase": "^9.0.2",
    "table": "^6.7.1"
  },
  "scripts": {
    "build": "npm run build:server",
    "build:server": "netlify-lambda build netlify/functions"
  },
  "devDependencies": {
    "netlify-lambda": "^2.0.14"
  }
}
